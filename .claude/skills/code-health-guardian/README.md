# Code Health Guardian - 代码健康守护者

**版本**: 1.0.0
**更新日期**: 2025-01-04

## 概述

Code Health Guardian 是一个专业的代码质量管理 skill，帮助开发团队：

- 🔍 **检测问题** - 重复代码、高复杂度、代码异味
- 🛠️ **提供方案** - 针对性的重构建议
- 📊 **生成报告** - 可视化的健康报告
- 📝 **管理债务** - 技术债务跟踪与飞书同步
- 🏗️ **规划演进** - 渐进式架构改进

## 快速开始

### 1. 代码体检

```bash
# 全量检查
/health check

# 检查指定目录
/health check src/modules/order

# 只显示严重问题
/health check --severity high
```

### 2. 检测重复代码

```bash
# 检测重复
/health duplicates

# 带重构建议
/health duplicates --suggest-refactor
```

### 3. 分析复杂度

```bash
# 显示最复杂的 10 个函数
/health complexity --top 10
```

### 4. 技术债务管理

```bash
# 添加债务
/health debt add "OrderService 需要拆分" --priority high

# 查看债务
/health debt list

# 同步到飞书
/health debt sync
```

### 5. 生成报告

```bash
# 生成 Markdown 报告
/health report

# 上传到飞书
/health report --upload
```

## 项目配置

在项目根目录创建 `.code-health.json`：

```json
{
  "projectType": "fullstack",
  "frontend": {
    "type": "react",
    "srcDir": "src"
  },
  "backend": {
    "type": "java",
    "srcDir": "src/main/java"
  },
  "thresholds": {
    "maxFileLines": 300,
    "maxFunctionLines": 50,
    "maxComplexity": 10
  }
}
```

## 支持的项目类型

| 类型 | 语言 | 框架 |
|------|------|------|
| 前端 | TypeScript/JavaScript | React |
| 后端 | Java | Spring Boot |

## 飞书集成

技术债务可同步到飞书多维表格：

1. 配置 `lark-config.json`
2. 运行 `/health debt sync`
3. 在飞书中查看和管理债务

## 文件结构

```
.code-health/
├── debts.json          # 本地债务存储
├── reports/            # 历史报告
│   ├── report-2025-01-04.md
│   └── report-2025-01-04.json
└── cache/              # 检测缓存
```

## 检测规则

- **React 规则**: `rules/react-rules.yaml`
- **Java 规则**: `rules/java-rules.yaml`

可根据项目需求自定义阈值和规则。

## 最佳实践

1. **日常开发**: 提交前运行 `/health check`
2. **周报**: 生成报告，评审技术债务
3. **迭代规划**: 将高优先级债务纳入迭代
4. **持续改进**: 逐步偿还债务，监控趋势

## 更多信息

详细文档见 `SKILL.md`
