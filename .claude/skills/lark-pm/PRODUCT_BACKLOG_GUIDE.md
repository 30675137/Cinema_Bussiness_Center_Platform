# Product Backlog 管理指南

**@spec T004-lark-project-management**

## 概述

使用飞书多维表格的 **Product Backlog (产品待办列表)** 统一管理 spec、plan、task 的完整生命周期。

## 数据模型

### Backlog 类型层级

```
Epic (史诗)
  └── User Story (用户故事)
       ├── Plan (实施计划) - 通过描述字段标记 **Plan**
       └── Task (开发任务) - 通过描述字段标记 **Task**
  └── Spike (技术调研/技术债)
```

### 字段说明

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 标题 | 文本 | Backlog 项标题 |
| 类型 | 单选 | Epic / User Story / Spike |
| 优先级 | 单选 | 🔴 P0 / 🟠 P1 / 🟡 P2 / 🟢 P3 |
| 状态 | 单选 | 📝 待规划 / 🎯 已规划 / 🚀 开发中 / ✅ 已完成 / ❌ 已废弃 |
| 故事点 | 单选 | 1 / 2 / 3 / 5 / 8 / 13 / 21 (斐波那契数列) |
| 负责人 | 人员 | 负责该 Backlog 项的人员 |
| spec_id | 文本 | 规格标识符 (如 T004, I003) |
| 验收标准 | 文本 | 完成的验收标准 |
| 描述 | 文本 | 详细描述，Plan/Task 类型会在开头添加 **Plan** 或 **Task** 标记 |
| 业务价值 | 数字 | 业务价值评分 (0-100) |

## 使用场景

### 1. 管理完整的 Spec (Epic)

```bash
# 创建 Epic 代表整个 spec
/lark-pm backlog smart-create "Spec T004: Lark 项目管理集成" --spec-id T004
```

**关键词**: spec, 规格, 功能集, 大型功能, 史诗, epic

**用途**:
- 管理完整的功能规格
- 作为 User Story 和 Spike 的父级容器
- 跟踪大型功能的整体进度

### 2. 管理实施计划 (Plan)

```bash
# 创建 Plan (User Story + **Plan** 标记)
/lark-pm backlog smart-create "制定 Product Backlog 管理的实施计划" --spec-id T004
```

**关键词**: plan, 计划, 规划, 实施计划, 设计方案

**特征**:
- 类型: User Story
- 描述开头: `**Plan**\n\n<实际内容>`
- 用于规划阶段的设计和方案

### 3. 管理开发任务 (Task)

```bash
# 创建 Task (User Story + **Task** 标记)
/lark-pm backlog smart-create "实现 smart-create 命令的自然语言解析功能" --spec-id T004
```

**关键词**: task, 任务, 待办, 实现, 开发, 修复

**特征**:
- 类型: User Story
- 描述开头: `**Task**\n\n<实际内容>`
- 代表具体的开发任务

### 4. 管理技术债 (Spike)

```bash
# 创建 Spike 代表技术债或技术调研
/lark-pm backlog smart-create "需要解决飞书MCP导入Markdown文档的能力"
```

**关键词**: 技术债, 需要解决, 缺少, 不支持, 性能优化, 架构优化

**用途**:
- 技术债务
- 技术调研
- 架构改进
- 性能优化

## 智能识别规则

### 优先级检测

输入中包含的关键词会自动映射优先级：

| 关键词 | 优先级 |
|--------|--------|
| 紧急, 高优先级, p0 | 🔴 P0 |
| 重要, p1 | 🟠 P1 |
| 中等, 普通, p2 | 🟡 P2 (默认) |
| 低, p3 | 🟢 P3 |

### 类型识别优先级

1. **Epic** - 如果包含 spec/规格/功能集 等关键词
2. **Spike** - 如果包含 技术债/需要解决/缺少 等关键词
3. **User Story (Plan)** - 如果包含 plan/计划/规划 等关键词
4. **User Story (Task)** - 如果包含 task/任务/实现 等关键词
5. **User Story** - 默认类型

## 工作流示例

### Spec 完整生命周期

```bash
# 1. 创建 Epic 代表整个 spec
/lark-pm backlog smart-create "Spec T004: Lark 项目管理集成" --spec-id T004

# 2. 创建 Plan 规划实施方案
/lark-pm backlog smart-create "制定 Backlog 管理的数据模型设计计划" --spec-id T004

# 3. 创建具体开发任务
/lark-pm backlog smart-create "实现 smart-create 命令" --spec-id T004
/lark-pm backlog smart-create "实现 backlog list 命令" --spec-id T004
/lark-pm backlog smart-create "实现 backlog update 命令" --spec-id T004

# 4. 发现技术债
/lark-pm backlog smart-create "需要解决飞书MCP导入Markdown文档的能力"
```

### 查询和过滤

```bash
# 查看所有 T004 相关的 Backlog
/lark-pm backlog list --spec-id T004

# 查看所有待规划的项目
/lark-pm backlog list --status "📝 待规划"

# 查看高优先级的项目
/lark-pm backlog list --priority "🔴 P0"

# 查看 Epic 类型
/lark-pm backlog list --type Epic
```

## 与传统 Scrum 的对应关系

| Product Backlog 类型 | Scrum 术语 | 说明 |
|---------------------|-----------|------|
| Epic | Epic | 大型功能，通常需要多个 Sprint |
| User Story | User Story | 用户故事，一个 Sprint 可完成 |
| User Story (**Plan**) | - | 实施计划，非标准 Scrum 术语，用于规划阶段 |
| User Story (**Task**) | Task | 具体开发任务，Sprint 内的执行单元 |
| Spike | Spike | 技术调研或技术债，用于探索性工作 |

## 故事点估算

使用斐波那契数列估算工作量：

- **1**: 极小任务 (< 2 小时)
- **2**: 小任务 (2-4 小时)
- **3**: 中小任务 (半天)
- **5**: 中等任务 (1 天)
- **8**: 较大任务 (2 天)
- **13**: 大任务 (3-4 天)
- **21**: 非常大的任务 (1 周)，建议拆分为更小的 User Story

## 状态转换流程

```
📝 待规划 → 🎯 已规划 → 🚀 开发中 → ✅ 已完成
                    ↓
                ❌ 已废弃
```

## 最佳实践

### 1. Epic 管理

- ✅ 一个 spec 对应一个 Epic
- ✅ Epic 的 spec_id 与 spec 文件名一致 (如 T004)
- ✅ Epic 下包含多个 User Story 和 Spike
- ❌ 避免 Epic 过大，建议拆分为多个子 Epic

### 2. Plan 管理

- ✅ 每个 Epic 至少有一个 Plan 描述实施方案
- ✅ Plan 应该包含设计思路、技术选型、架构决策
- ✅ Plan 完成后才创建具体的 Task
- ❌ 避免 Plan 过于抽象，应包含可执行的细节

### 3. Task 管理

- ✅ Task 应该足够小，能在 1-3 天内完成
- ✅ Task 标题使用动词开头 (实现、修复、优化、测试)
- ✅ Task 关联到具体的 spec_id
- ❌ 避免 Task 依赖过多，应尽量独立

### 4. Spike 管理

- ✅ 技术债应该有明确的解决方案描述
- ✅ Spike 应该有时间限制 (timeboxed)
- ✅ Spike 完成后应转化为 User Story 或 Task
- ❌ 避免 Spike 变成无限期的调研

## 命令参考

### 创建 Backlog

```bash
# 基本创建
/lark-pm backlog smart-create "<自然语言描述>"

# 指定类型
/lark-pm backlog smart-create "<描述>" --type Epic

# 指定优先级
/lark-pm backlog smart-create "<描述>" --priority P0

# 指定 spec ID
/lark-pm backlog smart-create "<描述>" --spec-id T004

# 组合使用
/lark-pm backlog smart-create "紧急：修复库存同步Bug" --priority P0 --spec-id I003
```

### 查询 Backlog

```bash
# 列出所有 Backlog
/lark-pm backlog list

# 按 spec 筛选
/lark-pm backlog list --spec-id T004

# 按状态筛选
/lark-pm backlog list --status "🚀 开发中"

# 按优先级筛选
/lark-pm backlog list --priority "🔴 P0"

# 按类型筛选
/lark-pm backlog list --type Epic

# 分页查询
/lark-pm backlog list --page 2 --page-size 20
```

### 更新 Backlog

```bash
# 更新状态
/lark-pm backlog update <record_id> --status "🚀 开发中"

# 更新优先级
/lark-pm backlog update <record_id> --priority "🔴 P0"

# 更新负责人
/lark-pm backlog update <record_id> --assignee <open_id>
```

## 总结

通过 Product Backlog 表统一管理 spec、plan、task，实现了：

1. **层级清晰**: Epic → User Story (Plan/Task) → Spike
2. **易于追踪**: 通过 spec_id 关联，通过状态跟踪进度
3. **智能识别**: 自然语言输入自动分类，减少手工操作
4. **符合 Scrum**: 遵循敏捷开发最佳实践
5. **灵活扩展**: 可通过描述字段标记添加更多子类型

---

**最后更新**: 2026-01-01
**版本**: 1.0.0
