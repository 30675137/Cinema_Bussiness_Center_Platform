#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 检查提交消息格式
commit_regex='^(feat|fix|docs|style|refactor|test|chore)(\(.+\))?: .{1,50}'

if ! grep -qE "$commit_regex" "$1"; then
    echo "❌ 提交消息格式不正确!"
    echo "📝 请使用以下格式:"
    echo "   feat: 新功能"
    echo "   fix: 修复bug"
    echo "   docs: 文档更新"
    echo "   style: 代码格式调整"
    echo "   refactor: 代码重构"
    echo "   test: 测试相关"
    echo "   chore: 构建过程或辅助工具的变动"
    echo ""
    echo "💡 支持范围标识: feat(component): 新功能"
    echo ""
    exit 1
fi

echo "✅ 提交消息格式验证通过!"