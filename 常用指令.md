# Claude Code GLM 模型快捷命令

## 使用方法

alias 已自动添加到 `~/.zshrc`，你可以直接在终端使用：

```bash
cc-glm
```

## Alias 定义

```bash
alias cc-glm="ANTHROPIC_AUTH_TOKEN=f40832ea44204eb9a91ae1751f70eac6.mPvF5krl5N7wsEqq \
  ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/anthropic \
  ANTHROPIC_SMALL_FAST_MODEL=GLM-4.6 \
  ANTHROPIC_MODEL=GLM-4.6 \
  claude --dangerously-skip-permissions"
```

```bash
alias cc-claude="ANTHROPIC_AUTH_TOKEN=sk-or-v1-0fecd19bdd6c232ed5ae6ee83d9569e905f6f7460e2b1796ce6d184f48a7886: \
  ANTHROPIC_BASE_URL=https://openrouter.ai/api/v1/chat/completions \
  ANTHROPIC_SMALL_FAST_MODEL=anthropic/claude-sonnet-4.5 \
  ANTHROPIC_MODEL=anthropic/claude-sonnet-4.5 \
  claude --dangerously-skip-permissions"
```

## 说明

- ✅ **已自动加载**：每次打开新终端时，这个 alias 会自动生效
- ✅ **当前会话**：如果是在当前终端，运行 `source ~/.zshrc` 即可立即使用
- 🚀 **直接使用**：只需输入 `cc-glm` 即可启动 Claude Code，无需手动设置环境变量

## 验证

检查 alias 是否生效：
```bash
alias cc-glm
```

如果显示 alias 定义，说明配置成功！