# CC-Presets 配置文件
# 包含敏感的 API 密钥和令牌，不应提交到版本控制

# 用户配置文件（包含敏感凭证）
.config/cc-presets/config.json

# 备份文件
.config/cc-presets/config.json.bak
.config/cc-presets/*.tmp
.config/cc-presets/*.backup

# 日志文件
.config/cc-presets/*.log
