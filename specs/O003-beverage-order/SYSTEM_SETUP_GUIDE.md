# O003-饮品订单管理 - US3 B端饮品配置管理 - 系统操作指南

**规格标识**: O003-beverage-order
**用户故事**: US3 - B端饮品配置管理
**最后更新**: 2025-12-28

---

## 一、系统页面架构

### 1.1 前端页面入口

#### 主列表页面
- **路径**: `/beverage/list`
- **组件**: `BeverageListPage`
- **文件**: `frontend/src/features/beverage-config/pages/BeverageListPage.tsx`

#### 导航菜单位置
**AppLayout 主菜单** (影院业务中台完整菜单):
```
├── O003-饮品订单管理 (CoffeeOutlined)
│   ├── US3-饮品配置管理 (/beverage/list) ✅ 已实现
│   └── US2-饮品订单/出品 (/beverage/orders) 📝 待实现
```

**Sidebar 菜单** (简化版菜单):
```
├── 饮品管理 (CoffeeOutlined)
│   └── 饮品配置 (/beverage/list) ✅ 已实现
```

---

## 二、功能页面结构

### 2.1 饮品列表页 (BeverageListPage)

#### 顶部操作区
```
┌─────────────────────────────────────────────────────────────┐
│  [搜索框: 饮品名称] [分类筛选▼] [状态筛选▼] [重置] [刷新]    │
│                                      [导出数据] [+ 新增饮品]  │
└─────────────────────────────────────────────────────────────┘
```

**功能说明**:
- **搜索框**: 支持饮品名称模糊搜索
- **分类筛选**: 全部分类 / 咖啡 / 茶饮 / 果汁 / 其他
- **状态筛选**: 全部状态 / 上架 / 下架
- **重置按钮**: 清空所有筛选条件
- **刷新按钮**: 重新加载列表数据
- **导出数据**: 导出饮品数据为 Excel（待实现）
- **新增饮品**: 打开饮品表单弹窗

#### 数据表格
```
┌────┬────────┬────┬────┬────┬────┬────┬──────┬────────────────────────┐
│主图│饮品名称│分类│价格│规格│配方│状态│创建时间│操作                    │
├────┼────────┼────┼────┼────┼────┼────┼──────┼────────────────────────┤
│ 🖼️ │美式咖啡│咖啡│¥18│ 4  │ 2  │上架│2025...│[编辑][规格][配方][下架]│
│    │ 推荐   │    │    │    │    │    │      │[删除]                  │
└────┴────────┴────┴────┴────┴────┴────┴──────┴────────────────────────┘
```

**表格字段**:
- **主图**: 饮品缩略图（60x60px），点击可预览
- **饮品名称**: 显示名称 + 推荐标签（金色 Tag）
- **分类**: Tag 标签（咖啡-棕色、茶饮-绿色、果汁-橙色、其他-灰色）
- **基础价格**: 元为单位，格式化为 ¥xx.xx
- **规格数**: 已配置规格数量（蓝色 Tag）
- **配方数**: 已配置配方数量（青色 Tag）
- **状态**: 上架-绿色✓ / 下架-灰色✗
- **创建时间**: 格式化为本地时间
- **操作列**:
  - `[编辑]` - 打开饮品表单弹窗（编辑模式）
  - `[规格]` - 打开规格配置弹窗
  - `[配方]` - 打开配方配置弹窗
  - `[上架/下架]` - 切换饮品状态（带确认弹窗）
  - `[删除]` - 软删除饮品（带确认弹窗）

#### 分页组件
```
┌─────────────────────────────────────────────────┐
│ 共 156 条 | 每页 [20▼] 条 | [1][2][3]...[8] [跳转▼] │
└─────────────────────────────────────────────────┘
```

**功能说明**:
- **总条数**: 显示符合筛选条件的总记录数
- **每页条数**: 可选择 10/20/50/100 条
- **分页器**: 支持页码点击和快速跳转
- **显示范围**: 第 1-20 条 / 共 156 条

---

### 2.2 饮品表单弹窗 (BeverageFormModal)

#### 新增模式
```
┌─────────────────────── 新增饮品 ──────────────────────┐
│                                                        │
│  饮品名称: [_____________________] *                    │
│  饮品分类: [咖啡 ▼] *                                   │
│  基础价格: [______] 元 *                                │
│  饮品描述: [__________________________]                 │
│            [__________________________]                 │
│                                                        │
│  主图上传: [+] 上传图片 (必填) *                         │
│           ┌────────┐                                   │
│           │  🖼️    │ (预览缩略图)                       │
│           └────────┘                                   │
│                                                        │
│  详情图:   [+] 上传详情图 (最多5张)                      │
│           ┌────┐ ┌────┐ ┌────┐                        │
│           │ 🖼️ │ │ 🖼️ │ │ 🖼️ │                        │
│           └────┘ └────┘ └────┘                        │
│                                                        │
│  推荐标签: [ ] 设为推荐饮品                              │
│  初始状态: [下架 ▼]                                     │
│                                                        │
│                              [取消] [确定]              │
└────────────────────────────────────────────────────────┘
```

**表单字段**:
- **饮品名称**: 必填，最多 100 个字符
- **饮品分类**: 必填，下拉选择（咖啡/茶饮/果汁/其他）
- **基础价格**: 必填，数字输入，≥ 0，支持两位小数
- **饮品描述**: 可选，文本域，最多 500 个字符
- **主图上传**: 必填，单张图片，≤ 5MB，格式 JPG/PNG/WEBP
- **详情图**: 可选，最多 5 张，拖拽排序，单张 ≤ 5MB
- **推荐标签**: 可选，开关，默认关闭
- **初始状态**: 可选，下拉选择（上架/下架），默认"下架"

**验证规则**:
- 名称不能为空
- 分类必须选择
- 价格必须 ≥ 0
- 主图必须上传
- 详情图单张 ≤ 5MB，总数 ≤ 5 张

#### 编辑模式
```
┌─────────────────────── 编辑饮品 ──────────────────────┐
│  (表单结构同新增模式，回填当前饮品数据)                  │
│                                                        │
│  注意: 修改基础价格会影响所有规格的最终价格              │
│       (最终价格 = 基础价格 + 规格调价)                  │
│                                                        │
│                              [取消] [保存]              │
└────────────────────────────────────────────────────────┘
```

---

### 2.3 规格配置弹窗 (SpecConfigModal)

```
┌─────────────── 配置规格: 美式咖啡 ───────────────┐
│                                                  │
│  ┌──────────────────────────────────────┐      │
│  │ 规格列表                  [+ 新增规格] │      │
│  ├────┬────┬────┬────┬────┬────┬──────┤      │
│  │类型│名称│价格│默认│排序│描述│操作  │      │
│  ├────┼────┼────┼────┼────┼────┼──────┤      │
│  │SIZE│小杯│-2元│    │ 1  │300ml│[编辑]│      │
│  │SIZE│中杯│ 0元│ ✓  │ 2  │350ml│[编辑]│      │
│  │SIZE│大杯│+3元│    │ 3  │500ml│[编辑]│      │
│  ├────┼────┼────┼────┼────┼────┼──────┤      │
│  │TEMP│热  │ 0元│ ✓  │ 1  │     │[编辑]│      │
│  │TEMP│冰  │ 0元│    │ 2  │     │[编辑]│      │
│  ├────┼────┼────┼────┼────┼────┼──────┤      │
│  │SWEET│正常│ 0元│ ✓  │ 1  │     │[编辑]│      │
│  │SWEET│半糖│ 0元│    │ 2  │     │[编辑]│      │
│  │SWEET│无糖│ 0元│    │ 3  │     │[编辑]│      │
│  ├────┼────┼────┼────┼────┼────┼──────┤      │
│  │TOPP│无  │ 0元│ ✓  │ 1  │     │[编辑]│      │
│  │TOPP│珍珠│+3元│    │ 2  │     │[编辑]│      │
│  │TOPP│椰果│+3元│    │ 3  │     │[编辑]│      │
│  └────┴────┴────┴────┴────┴────┴──────┘      │
│                                                  │
│                                  [关闭]          │
└──────────────────────────────────────────────────┘
```

#### 新增规格表单
```
┌─────────────── 新增规格 ────────────────┐
│                                          │
│  规格类型: [SIZE ▼] *                     │
│           (大小/温度/甜度/配料)            │
│                                          │
│  规格名称: [_________] *                  │
│           (如: 中杯、冰、半糖、珍珠)       │
│                                          │
│  价格调整: [______] 元 *                  │
│           (支持负数，如 -2 表示减价2元)    │
│                                          │
│  是否默认: [ ] 设为默认选项                │
│           (同类型只能有一个默认)           │
│                                          │
│  排序序号: [___] (数字越小越靠前)          │
│                                          │
│  规格描述: [_____________________]        │
│           (如: 350ml)                     │
│                                          │
│                      [取消] [确定]        │
└──────────────────────────────────────────┘
```

**规格类型说明**:
- **SIZE** (大小): 小杯、中杯、大杯、超大杯
- **TEMPERATURE** (温度): 热、温、冰、去冰
- **SWEETNESS** (甜度): 正常糖、半糖、少糖、无糖
- **TOPPING** (配料): 无配料、珍珠、椰果、芋圆、布丁

**默认规格规则**:
- 每个规格类型只能有一个默认选项
- 设置新的默认项时，系统自动取消同类型其他规格的默认标识
- C端 下单时默认选中默认规格

**价格计算示例**:
```
基础价格: ¥18
选择: 中杯(+0) + 冰(+0) + 半糖(+0) + 珍珠(+3)
最终价格: ¥18 + ¥0 + ¥0 + ¥0 + ¥3 = ¥21
```

---

### 2.4 配方配置弹窗 (RecipeConfigModal)

```
┌────────────── 配置配方: 美式咖啡 ──────────────┐
│                                                │
│  ┌───────────────────────────────────┐       │
│  │ 配方列表              [+ 新增配方] │       │
│  ├───────────────────────────────────┤       │
│  │ ▶ 中杯冰美式配方                   │       │
│  │   适用规格: {"SIZE":"中杯","TEMP":"冰"} │  │
│  │   原料清单:                         │       │
│  │   ┌────┬────┬────┬────┬────┬──┐ │       │
│  │   │SKU │名称│用量│单位│库存│操作│ │       │
│  │   ├────┼────┼────┼────┼────┼──┤ │       │
│  │   │1001│咖啡│ 20 │ g  │ 🟢 │编辑│ │       │
│  │   │1002│水  │200 │ ml │ 🟢 │编辑│ │       │
│  │   │1003│冰  │100 │ g  │ 🟡 │编辑│ │       │
│  │   └────┴────┴────┴────┴────┴──┘ │       │
│  │                            [编辑][删除]   │
│  └───────────────────────────────────┘       │
│                                                │
│  ┌───────────────────────────────────┐       │
│  │ ▶ 大杯热美式配方                   │       │
│  │   适用规格: {"SIZE":"大杯","TEMP":"热"} │  │
│  │   原料清单: (折叠)                  │       │
│  │                            [编辑][删除]   │
│  └───────────────────────────────────┘       │
│                                                │
│                                  [关闭]        │
└────────────────────────────────────────────────┘
```

#### 新增配方表单
```
┌──────────────── 新增配方 ────────────────┐
│                                            │
│  配方名称: [_______________] *              │
│           (如: 中杯冰美式配方)              │
│                                            │
│  适用规格: [_______________]                │
│           (JSON格式，可选)                  │
│           示例: {"SIZE":"中杯","TEMP":"冰"}  │
│                                            │
│  配方描述: [_____________________]          │
│           (如: 经典冰美式制作配方)          │
│                                            │
│  ┌─────────── 原料清单 ──────────┐        │
│  │                                │        │
│  │  SKU ID: [____] *               │        │
│  │  原料名: [____] *               │        │
│  │  用量:   [____] *               │        │
│  │  单位:   [____] *               │        │
│  │  备注:   [____]                 │        │
│  │                        [➖ 删除] │        │
│  │                                 │        │
│  │  SKU ID: [____] *               │        │
│  │  原料名: [____] *               │        │
│  │  用量:   [____] *               │        │
│  │  单位:   [____] *               │        │
│  │  备注:   [____]                 │        │
│  │                        [➖ 删除] │        │
│  │                                 │        │
│  │  [➕ 添加原料]                   │        │
│  └─────────────────────────────────┘        │
│                                            │
│                      [取消] [保存]          │
└────────────────────────────────────────────┘
```

**原料字段说明**:
- **SKU ID**: 必填，原料 SKU 编号，需在库存系统中存在
- **原料名称**: 必填，冗余存储，便于显示
- **用量**: 必填，数字，如 20、200
- **单位**: 必填，如 g、ml、kg、L
- **备注**: 可选，如"使用哥伦比亚咖啡豆"

**SKU 验证**:
- 添加原料时，系统自动校验 SKU ID 是否存在
- 不存在的 SKU ID 无法添加，提示"SKU 不存在"
- 显示库存状态:
  - 🟢 充足: 库存 > 安全库存
  - 🟡 紧张: 库存 ≤ 安全库存
  - 🔴 缺货: 库存 = 0

**适用规格 JSON 格式**:
```json
{
  "SIZE": "中杯",
  "TEMPERATURE": "冰"
}
```
- 留空表示适用于所有规格组合
- 可指定多个维度的规格组合

---

## 三、API 接口清单

### 3.1 饮品管理 API

| 接口功能 | HTTP方法 | 路径 | 请求体 | 响应体 |
|---------|---------|------|--------|--------|
| 获取饮品列表 | GET | `/api/admin/beverages?page=0&size=20&name=&category=&status=` | - | `Page<BeverageDTO>` |
| 获取饮品详情 | GET | `/api/admin/beverages/{id}` | - | `BeverageDetailDTO` |
| 创建饮品 | POST | `/api/admin/beverages` | `CreateBeverageRequest` | `BeverageDTO` |
| 更新饮品 | PUT | `/api/admin/beverages/{id}` | `UpdateBeverageRequest` | `BeverageDTO` |
| 删除饮品 | DELETE | `/api/admin/beverages/{id}` | - | `void` |
| 切换状态 | PATCH | `/api/admin/beverages/{id}/status?status=ACTIVE` | - | `BeverageDTO` |
| 上传图片 | POST | `/api/admin/beverages/upload-image` | `MultipartFile` | `String (URL)` |

### 3.2 规格管理 API

| 接口功能 | HTTP方法 | 路径 | 请求体 | 响应体 |
|---------|---------|------|--------|--------|
| 获取规格列表 | GET | `/api/admin/beverages/{beverageId}/specs` | - | `List<BeverageSpecDTO>` |
| 添加规格 | POST | `/api/admin/beverages/{beverageId}/specs` | `CreateSpecRequest` | `BeverageSpecDTO` |
| 更新规格 | PUT | `/api/admin/beverages/{beverageId}/specs/{specId}` | `UpdateSpecRequest` | `BeverageSpecDTO` |
| 删除规格 | DELETE | `/api/admin/beverages/{beverageId}/specs/{specId}` | - | `void` |

### 3.3 配方管理 API

| 接口功能 | HTTP方法 | 路径 | 请求体 | 响应体 |
|---------|---------|------|--------|--------|
| 获取配方列表 | GET | `/api/admin/beverages/{beverageId}/recipes` | - | `List<BeverageRecipeDTO>` |
| 添加配方 | POST | `/api/admin/beverages/{beverageId}/recipes` | `CreateRecipeRequest` | `BeverageRecipeDTO` |
| 更新配方 | PUT | `/api/admin/beverages/{beverageId}/recipes/{recipeId}` | `UpdateRecipeRequest` | `BeverageRecipeDTO` |
| 删除配方 | DELETE | `/api/admin/beverages/{beverageId}/recipes/{recipeId}` | - | `void` |

---

## 四、启动与访问

### 4.1 后端启动

```bash
cd backend
mvn spring-boot:run
```

**访问地址**: http://localhost:8080

**健康检查**: http://localhost:8080/actuator/health

### 4.2 前端启动

```bash
cd frontend
npm run dev
```

**访问地址**: http://localhost:5173

**饮品管理页面**: http://localhost:5173/beverage/list

### 4.3 完整访问流程

1. **启动后端服务**: `mvn spring-boot:run` (端口 8080)
2. **启动前端服务**: `npm run dev` (端口 5173)
3. **打开浏览器**: http://localhost:5173
4. **登录系统**: (如需登录)
5. **导航菜单**:
   - AppLayout: `O003-饮品订单管理` → `US3-饮品配置管理`
   - Sidebar: `饮品管理` → `饮品配置`
6. **进入列表页**: http://localhost:5173/beverage/list

---

## 五、测试场景

### 场景 1: 查看饮品列表
1. 访问 `/beverage/list`
2. 查看表格显示所有饮品
3. 使用搜索框搜索"美式"
4. 使用分类筛选选择"咖啡"
5. 使用状态筛选选择"上架"
6. 点击重置按钮清空筛选

### 场景 2: 新增饮品
1. 点击"新增饮品"按钮
2. 填写表单:
   - 名称: 美式咖啡
   - 分类: 咖啡
   - 价格: 18.00
   - 描述: 经典美式咖啡
   - 上传主图
   - 上传详情图
   - 设为推荐
   - 初始状态: 下架
3. 点击"确定"保存
4. 列表刷新显示新饮品

### 场景 3: 配置规格
1. 选择"美式咖啡"，点击"规格"按钮
2. 添加大小规格:
   - 小杯: -2元, 排序1
   - 中杯: 0元, 默认, 排序2
   - 大杯: +3元, 排序3
3. 添加温度规格:
   - 热: 0元, 默认, 排序1
   - 冰: 0元, 排序2
4. 添加甜度规格:
   - 正常糖: 0元, 默认, 排序1
   - 半糖: 0元, 排序2
   - 无糖: 0元, 排序3
5. 添加配料规格:
   - 无配料: 0元, 默认, 排序1
   - 珍珠: +3元, 排序2
   - 椰果: +3元, 排序3
6. 保存成功

### 场景 4: 配置配方
1. 选择"美式咖啡"，点击"配方"按钮
2. 添加配方:
   - 名称: 中杯冰美式配方
   - 适用规格: `{"SIZE":"中杯","TEMPERATURE":"冰"}`
   - 描述: 经典冰美式制作配方
3. 添加原料:
   - SKU: 1001, 名称: 咖啡豆, 用量: 20, 单位: g
   - SKU: 1002, 名称: 纯净水, 用量: 200, 单位: ml
   - SKU: 1003, 名称: 冰块, 用量: 100, 单位: g
4. 保存成功

### 场景 5: 上架/下架
1. 选择"美式咖啡"，点击"上架"按钮
2. 确认上架
3. 状态变为"已上架"
4. 点击"下架"按钮
5. 确认下架
6. 状态变为"已下架"

### 场景 6: 删除饮品
1. 选择某个饮品，点击"删除"按钮
2. 弹窗提示: "确认删除该饮品吗?删除后无法恢复,且关联的规格和配方也会被删除。"
3. 点击"确认删除"
4. 饮品软删除成功
5. 列表刷新,饮品不再显示

---

## 六、常见问题

### Q1: 为什么无法访问 /beverage/list 页面?
**A**: 检查:
1. 前端服务是否启动 (`npm run dev`)
2. 路由配置是否正确 (`frontend/src/router/index.tsx`)
3. 浏览器控制台是否有错误

### Q2: 列表页面显示空白?
**A**: 检查:
1. 后端服务是否启动 (`mvn spring-boot:run`)
2. 浏览器 Network 是否请求成功
3. 后端控制台是否有异常日志

### Q3: 上传图片失败?
**A**: 检查:
1. 文件大小是否 ≤ 5MB
2. 文件格式是否为 JPG/PNG/WEBP
3. 网络连接是否正常
4. Supabase Storage 是否已配置（待集成）

### Q4: 无法删除饮品?
**A**: 可能原因:
1. 饮品存在未完成订单（待集成订单模块校验）
2. 权限不足（需要 `beverage:delete` 权限）

### Q5: 导航菜单找不到"饮品管理"?
**A**: 检查:
1. AppLayout 菜单配置是否正确 (`frontend/src/components/layout/AppLayout.tsx`)
2. Sidebar 菜单配置是否正确 (`frontend/src/components/layout/Sidebar.tsx`)
3. CoffeeOutlined 图标是否正确导入

---

**文档版本**: v1.0.0
**最后更新**: 2025-12-28
**维护者**: 开发团队
