# 场景包管理 MVP 使用指南

**版本**: MVP v1.0
**日期**: 2025-12-19
**状态**: ✅ 编辑功能已完成

---

## 🎯 功能概览

### ✅ 已实现功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 场景包列表查询 | ✅ 完整 | 分页、排序、状态标签 |
| 创建场景包 | ✅ 完整 | 基本信息 + 规则配置 |
| 编辑场景包 | ✅ **新增** | 数据加载、表单预填充、乐观锁 |
| 删除场景包 | ✅ 完整 | 软删除 + 确认提示 |
| 预览场景包 | 🔶 占位符 | 后续实现 |

---

## 🚀 快速开始

### 1. 前置条件

#### 数据库迁移

在 Supabase SQL Editor 中执行迁移脚本：

```sql
-- 文件位置: backend/src/main/resources/db/migration/V1__create_scenario_packages.sql
-- 直接复制该文件内容到 Supabase SQL Editor 执行
```

#### Supabase Storage 配置

1. 进入 Supabase Dashboard → Storage
2. 创建新 Bucket：
   - Name: `scenario-packages`
   - Public: ✅ 勾选
   - File size limit: 5MB
   - Allowed MIME types: `image/jpeg, image/png, image/webp`

### 2. 启动应用

#### 后端

```bash
cd backend
./mvnw spring-boot:run

# 或使用 IDE 直接运行 Spring Boot Application
```

**后端地址**: `http://localhost:8080`

#### 前端

```bash
cd frontend
npm install  # 首次运行
npm run dev
```

**前端地址**: `http://localhost:5173`

### 3. 访问场景包管理

浏览器打开：`http://localhost:5173/scenario-packages`

---

## 📖 使用教程

### 场景 1: 创建新场景包

1. **进入列表页**
   - 访问 `/scenario-packages`
   - 点击右上角「新建场景包」按钮

2. **填写基本信息**
   ```
   场景包名称: VIP生日派对专场
   描述: 适合10-20人的生日派对，包含私人影厅和定制服务
   背景图片URL: https://picsum.photos/800/600
   ```

3. **配置使用规则**
   ```
   时长: 3 小时
   最小人数: 10 人
   最大人数: 20 人
   ```

4. **配置影厅类型（MVP限制）**
   - 当前需要手动输入影厅类型的 UUID
   - 测试示例：`123e4567-e89b-12d3-a456-426614174000`
   - 多个UUID用逗号分隔：`uuid1,uuid2,uuid3`

5. **保存**
   - 点击「保存草稿」
   - 自动跳转回列表页
   - 查看新创建的场景包

### 场景 2: 编辑现有场景包

1. **进入编辑页**
   - 在列表页找到要编辑的场景包
   - 点击「编辑」按钮

2. **查看元数据**
   - 页面顶部显示场景包的元数据：
     - ID、状态、版本号
     - **乐观锁版本号**（重要！）
     - 创建/更新时间

3. **修改信息**
   - 表单已预填充现有数据
   - 可修改任何字段：
     - 名称、描述
     - 背景图片URL
     - 时长、人数范围
     - 影厅类型

4. **预览当前图片**
   - 如果已设置背景图片，会显示预览
   - 无需重新输入URL（除非要更换）

5. **保存更新**
   - 点击「保存更新」
   - 后端会检查乐观锁版本号
   - 成功后自动跳转回列表

6. **处理并发冲突**
   - 如果其他用户同时修改：
     - 保存时会收到错误提示：「该场景包已被他人修改，请刷新后重试」
     - 页面会自动重新加载最新数据
     - 重新编辑并保存即可

### 场景 3: 删除场景包

1. **选择要删除的场景包**
   - 在列表页找到目标场景包

2. **确认删除**
   - 点击「删除」按钮
   - 弹出确认对话框：「确定要删除这个场景包吗？」
   - 点击「确定」

3. **软删除机制**
   - 场景包不会物理删除
   - 仅设置 `deleted_at` 时间戳
   - 列表中不再显示

---

## 🔍 API 测试

### 使用 curl 测试后端 API

#### 1. 创建场景包

```bash
curl -X POST http://localhost:8080/api/scenario-packages \
  -H "Content-Type: application/json" \
  -d '{
    "name": "VIP生日派对专场",
    "description": "适合10-20人的生日派对",
    "backgroundImageUrl": "https://picsum.photos/800/600",
    "rule": {
      "durationHours": 3.0,
      "minPeople": 10,
      "maxPeople": 20
    },
    "hallTypeIds": ["123e4567-e89b-12d3-a456-426614174000"]
  }'
```

**期望响应**:
```json
{
  "data": {
    "id": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
    "name": "VIP生日派对专场",
    "status": "DRAFT",
    "version": 1,
    "versionLock": 0,
    ...
  },
  "timestamp": "2025-12-19T..."
}
```

#### 2. 查询场景包列表

```bash
curl "http://localhost:8080/api/scenario-packages?page=0&size=20"
```

**期望响应**:
```json
{
  "success": true,
  "data": [...],
  "total": 1,
  "timestamp": "..."
}
```

#### 3. 查询单个场景包

```bash
# 替换 {id} 为实际的 UUID
curl http://localhost:8080/api/scenario-packages/{id}
```

#### 4. 更新场景包

```bash
# 替换 {id} 和 versionLock
curl -X PUT http://localhost:8080/api/scenario-packages/{id} \
  -H "Content-Type: application/json" \
  -d '{
    "versionLock": 0,
    "name": "VIP生日派对专场（已更新）",
    "description": "新的描述",
    "rule": {
      "durationHours": 4.0,
      "minPeople": 8,
      "maxPeople": 25
    }
  }'
```

#### 5. 删除场景包

```bash
curl -X DELETE http://localhost:8080/api/scenario-packages/{id}
```

---

## ⚙️ 技术细节

### 乐观锁机制

**工作原理**:

1. **读取数据**：前端加载场景包时获取 `versionLock`（如：0）
2. **用户编辑**：用户在表单中修改数据
3. **提交更新**：前端携带原始 `versionLock: 0` 发送 PUT 请求
4. **后端验证**：
   - 后端检查数据库中的 `versionLock` 是否仍为 0
   - 如果匹配：更新成功，`versionLock` 自动递增为 1
   - 如果不匹配：返回 409 Conflict 错误

**并发场景示例**:

| 时间 | 用户A | 用户B | 数据库versionLock |
|------|-------|-------|-------------------|
| T1 | 读取（versionLock=0） | - | 0 |
| T2 | - | 读取（versionLock=0） | 0 |
| T3 | 提交（versionLock=0）✅ | - | 1 |
| T4 | - | 提交（versionLock=0）❌ | 1 |

用户B的提交会失败，因为数据库中的 `versionLock` 已变为 1。

### 前端错误处理

编辑页实现了完善的错误处理：

```typescript
// 乐观锁冲突处理
if (error.response?.status === 409) {
  message.error('该场景包已被他人修改，请刷新后重试');
  refetch(); // 自动重新加载最新数据
}
```

### 数据加载流程

1. **URL参数解析**：`useParams()` 获取 `id`
2. **数据查询**：`usePackageDetail(id)` 发起 API 请求
3. **表单预填充**：`useEffect` 监听数据变化，调用 `form.setFieldsValue()`
4. **提交更新**：收集表单数据 + `versionLock`，发送 PUT 请求

---

## 🐛 故障排查

### 问题 1: 后端启动失败

**症状**: Spring Boot 启动报错

**可能原因**:
- Supabase 配置未正确设置
- 数据库连接失败

**解决方案**:
```bash
# 检查 application.yml 配置
cat backend/src/main/resources/application.yml

# 确认 SUPABASE_URL 和 SUPABASE_SERVICE_ROLE_KEY 环境变量
echo $SUPABASE_URL
```

### 问题 2: 前端无法连接后端

**症状**: API 请求返回 404 或 CORS 错误

**解决方案**:
1. 确认后端运行在 `http://localhost:8080`
2. 检查前端 API 配置：
   ```bash
   # 查看 api.ts 中的 baseURL
   grep -n "baseURL" frontend/src/services/api.ts
   ```
3. 检查浏览器控制台的 Network 标签

### 问题 3: 列表页显示为空

**症状**: 列表页加载成功但没有数据

**可能原因**:
- 数据库为空（正常）
- 查询条件过滤掉了所有数据

**解决方案**:
1. 创建第一个场景包
2. 检查浏览器控制台是否有错误
3. 检查 API 响应：
   ```bash
   curl http://localhost:8080/api/scenario-packages
   ```

### 问题 4: 编辑页乐观锁冲突

**症状**: 每次保存都提示「已被他人修改」

**可能原因**:
- 前端没有正确传递 `versionLock`
- 数据被异常修改

**解决方案**:
1. 点击页面上的「刷新」按钮重新加载数据
2. 检查浏览器控制台的 Network 标签，查看 PUT 请求的 payload
3. 确认 `versionLock` 字段存在且为数字类型

---

## 📝 已知限制

### MVP 版本限制

1. **图片上传**
   - ❌ 不支持文件选择和上传
   - ✅ 支持输入图片 URL
   - 后续版本：集成 Supabase Storage 预签名 URL

2. **影厅选择**
   - ❌ 不支持下拉选择框
   - ✅ 支持手动输入 UUID（逗号分隔）
   - 后续版本：从影厅管理模块加载可选项

3. **预览页**
   - ❌ 仅占位符
   - 后续版本：显示完整的场景包信息预览

4. **状态管理**
   - ❌ 不支持发布/下架操作
   - ✅ 所有场景包创建后为草稿状态
   - 后续版本：实现完整状态流转（草稿→已发布→已下架）

5. **定价和内容**
   - ❌ User Story 2/3/4 功能未实现
   - 后续版本：
     - 配置硬权益/软权益/服务项目
     - 设置打包定价
     - 计算参考总价和优惠比例

---

## 🎓 最佳实践

### 1. 测试乐观锁

**操作步骤**:
1. 在浏览器 A 中打开编辑页
2. 在浏览器 B 中打开同一个场景包的编辑页
3. 在浏览器 A 中修改名称并保存（成功）
4. 在浏览器 B 中修改描述并保存（失败，提示冲突）
5. 在浏览器 B 中点击刷新，重新编辑并保存（成功）

### 2. 数据验证

**前端验证**:
- 场景包名称：必填，最多255字符
- 时长：必填，最小0.1小时
- 人数范围：可选，非负整数

**后端验证**:
- 使用 `@Valid` 注解自动验证
- Bean Validation 约束（`@NotBlank`, `@DecimalMin`, `@Min`）

### 3. 错误处理

**前端统一错误处理**:
```typescript
// 在 useUpdatePackage hook 中
onError: (error: any) => {
  if (error.response?.status === 409) {
    message.error('该场景包已被他人修改，请刷新后重试');
  } else {
    message.error(`更新失败: ${error.response?.data?.message}`);
  }
}
```

---

## 🚀 下一步开发

### 优先级排序

1. **P1 - 影厅选择器**
   - 集成现有的影厅数据
   - 实现多选下拉框
   - 替换手动输入 UUID

2. **P2 - 预览页**
   - 显示场景包完整信息
   - 只读模式展示

3. **P3 - 图片上传**
   - 集成 Supabase Storage
   - 预签名 URL 上传
   - 图片预览和裁剪

4. **P4 - User Story 2-4**
   - 内容配置（硬权益、软权益、服务）
   - 定价设置
   - 状态管理（发布/下架）

---

## 📞 支持

如有问题，请参考：
- 技术文档：`specs/017-scenario-package/`
- API 契约：`specs/017-scenario-package/contracts/api.yaml`
- 数据模型：`specs/017-scenario-package/data-model.md`

---

**文档版本**: MVP v1.0
**最后更新**: 2025-12-19
**作者**: Cinema Platform Team
